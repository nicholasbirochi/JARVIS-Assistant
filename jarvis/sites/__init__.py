"""Job-site automation adapters. `base.py` defines the `SiteAdapter`
interface and supporting types -- design only, no adapter is implemented
yet. See base.py's module docstring for the Gupy-first recommendation and
the dry-run/confirm-before-apply contract every future adapter must follow.
"""
